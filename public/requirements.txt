# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0          # serveur ASGI (reload, workers)

# ── MongoDB async ──────────────────────────────────────────────
motor==3.4.0                       # driver MongoDB asynchrone
pymongo==4.7.2                     # requis par motor

# ── Validation / sérialisation ────────────────────────────────
pydantic==2.7.1
pydantic[email]==2.7.1             # EmailStr (validation email)

# ── Authentification ──────────────────────────────────────────
bcrypt==4.1.3                      # hashage des mots de passe
PyJWT==2.8.0                       # création / vérification JWT

# ── Variables d'environnement ─────────────────────────────────
python-dotenv==1.0.1               # charge le fichier .env

# ── Upload de fichiers (images oeuvres / avatars) ─────────────
python-multipart==0.0.9            # requis par FastAPI pour Form() et File()